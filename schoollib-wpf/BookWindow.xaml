<Window x:Class="schoollib_wpf.BookWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:schoollib_wpf"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="BookWindow" Width="460" SizeToContent="Height">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical">
            <TextBlock Text="Название:"/>
            <TextBox x:Name="bookTitleTextBox" Margin="0,0,0,8"/>
            <TextBlock Text="Автор:"/>
            <TextBox x:Name="bookAuthorTextBox" Margin="0,0,0,8"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1">
            <Button x:Name="okButton" IsDefault="True" Content="ОК" Width="80" Margin="0,0,8,0" Click="okButton_Click"/>
            <Button x:Name="cancelButton" IsCancel="True" Content="Отмена" Width="80" Click="cancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
